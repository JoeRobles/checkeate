{% extends 'admin-layout.html.twig' %}

{% block page_title %}Editar orden{% endblock %}

{% block page_content %}
    <div class="box">
        <div class="box-body">
            <form action="{{ edit_form.vars.action }}" class="form-horizontal" {{ form_enctype(edit_form) }} method="{{ edit_form.vars.method }}" name="{{ edit_form.vars.id }}" role="form">
                {{ form_errors(edit_form) }}
                <div class="form-group{% if edit_form.subFormFactor.vars.errors[0].message is defined %} has-error{% endif %}">
                    {{ form_label(edit_form.subFormFactor, 'Producto', {'label_attr': {'class': 'col-sm-2 control-label'} }) }}
                    <div class="col-sm-10">
                        {{ form_widget(edit_form.subFormFactor, {'attr': {'class': 'form-control', 'required': true} }) }}
                        {% if edit_form.subFormFactor.vars.errors[0].message is defined %}
                            <span class="help-block">
                            {{ edit_form.subFormFactor.vars.errors[0].message }}
                        </span>
                        {% endif %}
                    </div>
                </div>
                <div class="form-group{% if edit_form.status.vars.errors[0].message is defined %} has-error{% endif %}">
                    {{ form_label(edit_form.status, 'Estado', {'label_attr': {'class': 'col-sm-2 control-label'} }) }}
                    <div class="col-sm-10">
                        {{ form_widget(edit_form.status, {'attr': {'class': 'form-control', 'required': true} }) }}
                        {% if edit_form.status.vars.errors[0].message is defined %}
                        <span class="help-block">
                            {{ edit_form.status.vars.errors[0].message }}
                        </span>
                        {% endif %}
                    </div>
                </div>
                <button class="btn btn-primary entity-submit pull-left" id="{{ edit_form.vars.id }}_submit" type="submit">Editar</button>
                {{ form_widget(edit_form._token) }}
            </form>
        </div>
        <div class="box-footer">
            {{ form_start(delete_form) }}
            <input class="btn btn-danger pull-left" type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
            <a class="btn btn-default pull-left" href="{{ path('orden_index') }}">Volver al listado</a>
        </div>
    </div>
{% endblock %}